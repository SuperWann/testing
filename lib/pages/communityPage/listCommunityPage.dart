import 'package:flutter/material.dart';

class ListCommunityPage extends StatelessWidget {
  const ListCommunityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Kenapa namanya List Community Page? karena isinya list group/community')));
  }
}